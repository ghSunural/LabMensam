'use strict';

//global.

/*загрузка настроек
var loadOptionsPromise = new Promise(function (resolve, reject) {

});
loadOptionsPromise.then(onFulfiled, onRejected, onProgress);

 */

/*
 var jsonExplorerObject = JSON.stringify(SurveyObject);
 fs.writeFile("test14.json", jsonExplorerObject, function (err) {
 if (err) {
 console.log(err);
 } else {
 console.log("Файл сохранен.");
 }
 });

 var jsonExplorerObject1 = fs.readFile('test4.json', {encoding: 'utf8'}, function (err, data) {
 if (err) throw err;
 console.log(data);
 });

 var jsonExplorerObject2 = JSON.parse(jsonExplorerObject1);
 console.log(jsonExplorerObject2);
 */

/*
 module.exports = {
 fp: pushContentToHtml(id, innerText),
 fc: clearContent(id)
 };
 */